{
  "name": "@stratix/queue",
  "version": "0.1.0",
  "description": "高性能、灵活的结构化消息栈",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "rimraf dist && tsup src/index.ts --format cjs,esm --dts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "logger",
    "logging",
    "typescript",
    "structured-logging",
    "pino"
  ],
  "author": "Stratix Team",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {},
  "peerDependencies": {
    "typescript": ">=4.5.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
