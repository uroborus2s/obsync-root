{
  "name": "@stratix/was-v7",
  "version": "1.0.0-beta.36",
  "description": "Stratix WPS V7 integration plugin with calendar, schedule and organization adapters",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist",
    "build": "pnpm run clean && tsc",
    "lint": "eslint src --ext .ts",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "axios": "^1.18.0",
    "zod": "^4.4.3"
  },
  "peerDependencies": {
    "@stratix/core": "workspace:*",
    "@stratix/redis": "workspace:*"
  },
  "devDependencies": {
    "@stratix/core": "workspace:*",
    "@stratix/redis": "workspace:*",
    "awilix": "^13.0.5"
  },
  "keywords": [
    "stratix",
    "wps",
    "wps-v7",
    "calendar",
    "schedule",
    "organization",
    "adapter",
    "plugin"
  ],
  "author": "Stratix Team",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
