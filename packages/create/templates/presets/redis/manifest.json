{
  "id": "redis",
  "version": "1.0.0",
  "displayName": "Redis Preset",
  "description": "Add @stratix/redis",
  "appliesTo": [
    { "kind": "app" },
    { "kind": "plugin" }
  ],
  "contributes": {
    "dependencies": {
      "runtime": {
        "@stratix/redis": "^1.0.0-beta.2"
      }
    }
  }
}
