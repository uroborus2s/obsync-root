{
  "id": "tasks",
  "version": "1.0.0",
  "displayName": "Tasks Preset (Deprecated)",
  "description": "Deprecated: keep only for legacy @stratix/tasks migration; do not use in new projects",
  "appliesTo": [
    { "kind": "app" },
    { "kind": "plugin" }
  ],
  "contributes": {
    "dependencies": {
      "runtime": {
        "@stratix/tasks": "^1.0.0-beta.5"
      }
    }
  }
}
