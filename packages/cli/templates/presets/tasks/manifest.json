{
  "id": "tasks",
  "version": "1.0.0",
  "displayName": "Tasks Preset",
  "description": "Add @stratix/tasks for task executor workflows",
  "appliesTo": [
    { "kind": "app" },
    { "kind": "plugin" }
  ],
  "contributes": {
    "dependencies": {
      "runtime": {
        "@stratix/tasks": "^1.0.0-beta.5"
      }
    }
  }
}
