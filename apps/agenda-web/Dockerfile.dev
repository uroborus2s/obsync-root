FROM node:22-alpine

# 安装pnpm
RUN corepack enable pnpm

# 设置工作目录
WORKDIR /app

# 复制package.json
COPY package.json pnpm-lock.yaml* ./

# 安装依赖
RUN pnpm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 5173

# 启动开发服务器
CMD ["pnpm", "run", "dev", "--host", "0.0.0.0"] 