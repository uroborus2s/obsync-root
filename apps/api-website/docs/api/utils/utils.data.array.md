<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@stratix/utils](./utils.md) &gt; [data](./utils.data.md) &gt; [array](./utils.data.array.md)

## data.array namespace

## Functions

<table><thead><tr><th>

Function


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[chunk(array, size)](./utils.data.array.chunk.md)


</td><td>

将数组分割成指定大小的多个小数组

将一个数组分割成指定大小的多个较小数组。如果数组不能被平均分割，最后一个数组将包含剩余元素。


</td></tr>
<tr><td>

[compact(array)](./utils.data.array.compact.md)


</td><td>

创建一个去除假值的新数组

从数组中移除所有假值。JavaScript 中的假值包括 `false`<!-- -->、`null`<!-- -->、`0`<!-- -->、`""`<!-- -->、 `undefined` 和 `NaN`<!-- -->。


</td></tr>
<tr><td>

[difference(array, values)](./utils.data.array.difference.md)


</td><td>

创建一个新数组，包含在第一个数组中但不在其他数组中的值

返回一个新数组，其中包含存在于第一个数组中但不存在于其他数组中的值。 使用Set数据结构实现高效比较。


</td></tr>
<tr><td>

[flatten(array, depth)](./utils.data.array.flatten.md)


</td><td>

将嵌套数组扁平化到指定深度

递归地将嵌套数组扁平化到指定的深度。


</td></tr>
<tr><td>

[groupBy(array, iteratee)](./utils.data.array.groupby.md)


</td><td>

按照指定条件对数组进行分组

创建一个对象，其键是对数组中每个元素执行迭代函数的结果，值是包含属于该分组的元素的数组。 支持传入字符串属性名或函数作为分组条件。


</td></tr>
<tr><td>

[intersection(arrays)](./utils.data.array.intersection.md)


</td><td>

计算多个数组的交集

创建一个新数组，包含所有传入数组中共有的元素。


</td></tr>
<tr><td>

[keyBy(array, iteratee)](./utils.data.array.keyby.md)


</td><td>

创建一个键值映射对象

创建一个对象，其键是对数组中每个元素执行迭代函数的结果，值是对应的元素。 如果多个元素产生相同的键，则后面的元素会覆盖前面的元素。


</td></tr>
<tr><td>

[partition(array, predicate)](./utils.data.array.partition.md)


</td><td>

将数组分割成两部分

根据断言函数将数组元素分为两个组：满足条件的元素和不满足条件的元素。


</td></tr>
<tr><td>

[reduce(array, iteratee, initialValue)](./utils.data.array.reduce.md)


</td><td>

对数组中的每个元素执行累积计算

从左到右对数组中的每个元素执行提供的函数，将其结果汇总为单个返回值。 本函数是原生Array.reduce的类型安全包装。


</td></tr>
<tr><td>

[shuffle(array)](./utils.data.array.shuffle.md)


</td><td>

创建随机排序的数组副本

使用Fisher-Yates洗牌算法创建原数组的随机排序副本。


</td></tr>
<tr><td>

[sortBy(array, iteratees)](./utils.data.array.sortby.md)


</td><td>

创建按照指定条件排序的数组副本

根据迭代函数的结果对数组元素进行排序。支持多个排序条件。


</td></tr>
<tr><td>

[take(array, n)](./utils.data.array.take.md)


</td><td>

获取数组的前N个元素

创建一个数组切片，包含从开头截取的N个元素。


</td></tr>
<tr><td>

[union(arrays)](./utils.data.array.union.md)


</td><td>

创建一个包含所有数组唯一值的数组

合并所有传入的数组，并返回一个包含所有唯一值的新数组。


</td></tr>
<tr><td>

[unique(array, iteratee)](./utils.data.array.unique.md)


</td><td>

创建一个去重后的数组

创建一个新数组，移除所有重复值，可以通过迭代函数指定唯一性判断条件。


</td></tr>
</tbody></table>
