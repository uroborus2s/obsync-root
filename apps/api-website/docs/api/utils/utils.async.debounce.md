<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [@stratix/utils](./utils.md) &gt; [async](./utils.async.md) &gt; [debounce](./utils.async.debounce.md)

## async.debounce() function

创建防抖函数，限制函数调用频率

防抖函数会延迟执行，如果在延迟时间内再次调用，则重新计时。 可选择是否在延迟开始前立即执行一次（leading）以及在延迟结束后执行（trailing）。

**Signature:**

```typescript
export declare function debounce<F extends (...args: any[]) => any>(func: F, wait?: number, options?: DebounceOptions): F & {
    cancel(): void;
    flush(): ReturnType<F>;
    pending(): boolean;
};
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

func


</td><td>

F


</td><td>

需要防抖的函数


</td></tr>
<tr><td>

wait


</td><td>

number


</td><td>

_(Optional)_ 延迟时间(毫秒)，默认为0


</td></tr>
<tr><td>

options


</td><td>

[DebounceOptions](./utils.async.debounceoptions.md)


</td><td>

_(Optional)_ 防抖选项


</td></tr>
</tbody></table>
**Returns:**

F &amp; { cancel(): void; flush(): ReturnType&lt;F&gt;; pending(): boolean; }

防抖后的函数，带有cancel方法用于取消等待的执行

## Remarks

版本: 1.0.0 分类: 函数调节

## Example


```typescript
// 基本使用 - 延迟处理用户输入
const debouncedSearch = debounce(
  (query) => {
    fetchSearchResults(query);
  },
  300 // 300ms延迟
);

// 监听输入事件
searchInput.addEventListener('input', (e) => {
  debouncedSearch(e.target.value);
});

// 高级用法 - 设置leading和maxWait
const handleResize = debounce(
  () => {
    recalculateLayout();
  },
  200,
  {
    leading: true,    // 首次立即执行
    trailing: true,   // 延迟后也执行
    maxWait: 1000     // 最多等待1秒
  }
);

// 取消挂起的执行
const debouncedSave = debounce(saveData, 1000);
debouncedSave(formData);

// 用户离开页面，取消保存
cancelButton.addEventListener('click', () => {
  debouncedSave.cancel();
});
```

