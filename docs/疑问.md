刚才碰了下有如下问题，还要思考下：

1. 打卡聚合表：
```sql
CREATE TABLE `punch_summary` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `xh` varchar(40) NOT NULL COMMENT '学生学号',
  `kkh` varchar(60) NOT NULL COMMENT '开课号',
  `xnxq` varchar(20) NOT NULL COMMENT '学年学期',
  `zc` int(11) NOT NULL COMMENT '周几',
  `jc` int(11) NOT NULL COMMENT '节次',
  `jxz_list` varchar(255) NOT NULL COMMENT '教学周列表，逗号分隔',
  `punch_status_list` varchar(255) NOT NULL COMMENT '打卡状态列表，对应jxz_list',
  `total_classes` int(11) DEFAULT 0 COMMENT '总课程数',
  `punched_count` int(11) DEFAULT 0 COMMENT '已打卡次数',
  `leave_count` int(11) DEFAULT 0 COMMENT '请假次数',
  `absent_count` int(11) DEFAULT 0 COMMENT '缺勤次数',
  `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_unique_summary` (`xh`, `kkh`, `xnxq`, `zc`, `jc`),
  KEY `idx_xh_kkh` (`xh`, `kkh`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='打卡聚合表';
```

这是对教学周（1-18周）做了聚合，其他字段都没有动。
节未合并，有一种合并方式是教学周和节都进行合并 类似于如下
| 节 | 教学周 |
|-------|-------|
| 1-2 | 1,2,3,4,5,6 |
如果有调课行为，在这个表上是如何思考？调课是否会影响统计（比如第三教学周的周1的1，2节调整到第四教学周周2的5，6节）。
2. 跟中间库所有课表同步的逻辑在文档中没有体现，需要详细的同步日志，校方不能处于黑盒状态，发现不了问题，是整体同步还是提前一周同步，同步推送课表日历是教师优先，学生次之没有标明。
3. 打卡任务表中并没有体现1，2，3，4，5节（假设前4节是上午，第5节是下午），应该只打卡1和5节，如果打卡了开头的节次，之后的2，3，4节就默认已经打卡。之前商量过向日历推送，可以把1-4节先合成一个日历块，第5节是一个日历块的方案，如何细化实现有没有考虑过？是通过系统配置表的节之间的间隔来控制的吗？
4. 对于3所列的打卡方案，我们是否可以简化一下，一门课一天只打卡一次。这样要求我们要通过对节进行日内聚合groupconcat，然后取聚合的第一位（1，2，3，4，5的1）进行打卡即可。当第一节打卡完毕，所有的都完成打卡。
5. 课表日历的提醒也跟打卡类似，1，2，3，4只提醒一次即可。
6. 请假记录表+关联表（关联表貌似没有开始结束时间进行锁定）