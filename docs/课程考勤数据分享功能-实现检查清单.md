# 课程考勤数据分享功能 - 实现检查清单

## 📋 总览

本文档提供完整的实现检查清单，确保所有功能模块都已正确实现。

---

## 1. 数据库层 ✅

### 1.1 创建数据表

- [ ] 创建 `icalink_attendance_export_records` 表
  - 文件: `apps/app-icalink/database/tables/icalink_attendance_export_records.sql`
  - 包含所有必需字段（task_id, export_type, query_hash等）
  - 创建所有必需索引

### 1.2 执行SQL脚本

```bash
# 在数据库中执行
mysql -u root -p icalink < apps/app-icalink/database/tables/icalink_attendance_export_records.sql
```

### 1.3 验证表结构

```sql
-- 验证表是否创建成功
DESCRIBE icalink_attendance_export_records;

-- 验证索引
SHOW INDEX FROM icalink_attendance_export_records;
```

---

## 2. 类型定义层 ✅

### 2.1 创建导出类型定义

- [ ] 创建 `apps/app-icalink/src/types/attendance-export.types.ts`
  - [ ] `AttendanceExportType` 枚举
  - [ ] `AttendanceExportStatus` 枚举
  - [ ] `IcalinkAttendanceExportRecord` 接口
  - [ ] `RealtimeExportRequest` 接口
  - [ ] `HistoryExportRequest` 接口
  - [ ] `ExportTaskResponse` 接口

### 2.2 更新数据库类型定义

- [ ] 在 `apps/app-icalink/src/types/database.ts` 中添加:
  ```typescript
  export interface IcalinkDatabase {
    // ... 现有表
    icalink_attendance_export_records: IcalinkAttendanceExportRecord;
  }
  ```

---

## 3. Repository层 ✅

### 3.1 创建AttendanceExportRecordRepository

- [ ] 文件: `apps/app-icalink/src/repositories/AttendanceExportRecordRepository.ts`
- [ ] 继承 `BaseRepository`
- [ ] 实现方法:
  - [ ] `findByTaskId(taskId: string)`
  - [ ] `findCompletedByQueryHash(queryHash: string)`
  - [ ] `updateTaskStatus(taskId, status, updates)`
  - [ ] `findExpiredRecords()`
  - [ ] `deleteExpiredRecords()`

### 3.2 注册到DI容器

- [ ] 在插件入口文件中配置自动发现
- [ ] 或手动注册到容器

---

## 4. Service层 ✅

### 4.1 创建AttendanceExportService

- [ ] 文件: `apps/app-icalink/src/services/AttendanceExportService.ts`
- [ ] 定义接口 `IAttendanceExportService`
- [ ] 实现方法:
  - [ ] `exportRealtimeData(request, userId)`
  - [ ] `exportHistoryData(request, userId)`
  - [ ] `getTaskStatus(taskId)`
  - [ ] `downloadFile(taskId)`
  - [ ] `generateQueryHash(params)` (私有方法)
  - [ ] `executeRealtimeExport(taskId, courseId)` (私有方法)
  - [ ] `executeHistoryExport(taskId, courseCode, sortField, sortOrder)` (私有方法)
  - [ ] `generateRealtimeExcel(data)` (私有方法)
  - [ ] `generateHistoryExcel(data)` (私有方法)

### 4.2 依赖注入

- [ ] 注入 `AttendanceExportRecordRepository`
- [ ] 注入 `AttendanceTodayViewRepository`
- [ ] 注入 `StudentAbsenceRateDetailService`
- [ ] 注入 `OsspStorageService`
- [ ] 注入 `Logger`

---

## 5. Executor层 ✅

### 5.1 创建AttendanceExportExecutor

- [ ] 文件: `apps/app-icalink/src/executors/AttendanceExportExecutor.ts`
- [ ] 使用 `@Executor` 装饰器
- [ ] 实现 `TaskExecutor` 接口
- [ ] 实现 `execute(context: ExecutionContext)` 方法
- [ ] 处理实时数据导出
- [ ] 处理历史数据导出
- [ ] 更新任务进度
- [ ] 错误处理和重试逻辑

### 5.2 配置元数据

```typescript
@Executor({
  name: 'attendanceExport',
  description: '考勤数据导出执行器',
  version: '1.0.0',
  tags: ['attendance', 'export', 'excel'],
  category: 'data-export'
})
```

---

## 6. Controller层 ✅

### 6.1 创建AttendanceExportController

- [ ] 文件: `apps/app-icalink/src/controllers/AttendanceExportController.ts`
- [ ] 使用 `@Controller` 装饰器
- [ ] 实现路由:

#### 6.1.1 导出实时数据

```typescript
@Post('/api/icalink/v1/attendance/export/realtime', {
  schema: { /* ... */ }
})
async exportRealtimeData(request, reply)
```

#### 6.1.2 导出历史数据

```typescript
@Post('/api/icalink/v1/attendance/export/history', {
  schema: { /* ... */ }
})
async exportHistoryData(request, reply)
```

#### 6.1.3 查询任务状态

```typescript
@Get('/api/icalink/v1/attendance/export/status/:taskId', {
  schema: { /* ... */ }
})
async getTaskStatus(request, reply)
```

#### 6.1.4 下载文件

```typescript
@Get('/api/icalink/v1/attendance/export/download/:taskId', {
  schema: { /* ... */ }
})
async downloadFile(request, reply)
```

### 6.2 Schema定义

- [ ] 完整的请求参数Schema
- [ ] 完整的响应格式Schema
- [ ] 参数验证规则

---

## 7. Excel生成逻辑 ✅

### 7.1 安装依赖

```bash
pnpm add exceljs
pnpm add -D @types/exceljs
```

### 7.2 实时数据Excel模板

- [ ] 表头设计（序号、学号、姓名、班级、专业、签到状态、签到时间、备注）
- [ ] 样式设置（表头加粗、背景色、数据行颜色）
- [ ] 状态转换（final_status → 中文）
- [ ] 时间格式化

### 7.3 历史统计数据Excel模板

- [ ] 表头设计（序号、学号、姓名、班级、总课次、已上课次、缺勤次数、请假次数、旷课次数、缺课率、旷课率、请假率）
- [ ] 样式设置（根据缺课率设置颜色）
- [ ] 百分比格式化
- [ ] 数据排序

---

## 8. OSS集成 ✅

### 8.1 文件上传

- [ ] 使用 `OsspStorageService.uploadImage()` 或自定义上传方法
- [ ] 文件路径规则: `attendance/{type}/{courseId|courseCode}/{timestamp|hash}.xlsx`
- [ ] 设置正确的 Content-Type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`

### 8.2 文件下载

- [ ] 使用 `OsspStorageService.downloadImage()` 或自定义下载方法
- [ ] 返回文件流给客户端
- [ ] 设置正确的响应头

---

## 9. 前端实现 ✅

### 9.1 创建分享对话框组件

- [ ] 文件: `apps/agendaedu-app/src/components/ShareAttendanceDialog.tsx`
- [ ] **状态1: 选择数据类型**
  - [ ] 实时数据选项（带图标和说明）
  - [ ] 历史统计数据选项（带图标和说明）
  - [ ] 关闭按钮
- [ ] **状态2: 进度显示**
  - [ ] 进度条（0-100%）
  - [ ] 进度百分比显示
  - [ ] 状态文字（正在查询数据、正在生成Excel、正在上传文件、即将完成）
- [ ] **状态3: 下载就绪**
  - [ ] 成功图标 ✅
  - [ ] 下载按钮
  - [ ] 文件信息（文件名、大小）
  - [ ] 缓存提示（仅历史数据显示）
- [ ] **状态4: 错误提示**
  - [ ] 错误图标和错误信息
  - [ ] 重试按钮
  - [ ] 关闭按钮

### 9.2 集成到AttendanceSheet

- [ ] **修改Tab导航栏布局**
  - [ ] Tab容器使用 `flex` 布局
  - [ ] Tab按钮使用 `flex-1` 占据左侧空间
  - [ ] 在Tab导航栏右侧添加"分享"按钮（固定位置）
  - [ ] 分享按钮使用 Share2 图标
- [ ] **添加状态管理**
  - [ ] `isShareDialogOpen` 状态
  - [ ] 打开/关闭对话框的处理函数
- [ ] **渲染对话框组件**
  - [ ] 传递必要的props（courseId, courseCode, courseName）
  - [ ] 处理对话框关闭事件

### 9.3 API客户端方法

- [ ] 在 `apps/agendaedu-app/src/lib/attendance-api.ts` 中添加:
  - [ ] `ExportTaskResponse` 接口定义
  - [ ] `exportRealtimeData(courseId)` - 导出实时数据
  - [ ] `exportHistoryData(courseCode, sortField, sortOrder)` - 导出历史数据
  - [ ] `getExportTaskStatus(taskId)` - 查询任务状态
  - [ ] `downloadExportFile(taskId, fileName)` - 下载文件

### 9.4 轮询逻辑

- [ ] 实现任务状态轮询（每2秒）
- [ ] 根据进度更新状态文字
  - [ ] 0-30%: "正在查询数据..."
  - [ ] 30-70%: "正在生成Excel..."
  - [ ] 70-90%: "正在上传文件..."
  - [ ] 90-100%: "即将完成..."
- [ ] 任务完成后停止轮询，显示下载按钮
- [ ] 任务失败后停止轮询，显示错误信息
- [ ] 组件卸载时清理定时器

### 9.5 缓存处理

- [ ] 历史数据导出时检查 `cacheHit` 标志
- [ ] 命中缓存时直接显示下载按钮（跳过进度条）
- [ ] 显示缓存提示信息
- [ ] 实时数据不使用缓存，始终显示进度条

---

## 10. 缓存策略 ✅

### 10.1 查询哈希生成

- [ ] 使用 `crypto.createHash('sha256')` 生成哈希
- [ ] 包含所有查询参数（courseCode, sortField, sortOrder）
- [ ] 确保参数顺序一致

### 10.2 缓存判断

- [ ] 查询 `query_hash` 相同且 `status='completed'` 且未过期的记录
- [ ] 命中缓存直接返回下载链接
- [ ] 未命中缓存创建新任务

### 10.3 文件过期清理

- [ ] 创建定时任务（每天凌晨执行）
- [ ] 删除过期的OSS文件
- [ ] 删除过期的数据库记录

---

## 11. 测试 ✅

### 11.1 单元测试

- [ ] Repository层测试
- [ ] Service层测试
- [ ] Executor层测试

### 11.2 集成测试

- [ ] 实时数据导出流程测试
- [ ] 历史数据导出流程测试（含缓存）
- [ ] 任务状态查询测试
- [ ] 文件下载测试

### 11.3 端到端测试

- [ ] 前端到后端完整流程测试
- [ ] 并发请求测试
- [ ] 错误场景测试

---

## 12. 性能优化 ✅

### 12.1 数据库优化

- [ ] 添加必要的索引
- [ ] 查询优化（避免全表扫描）
- [ ] 分页查询大数据集

### 12.2 Excel生成优化

- [ ] 使用流式写入（避免内存溢出）
- [ ] 批量处理数据
- [ ] 限制单个文件大小（最大10MB）

### 12.3 OSS优化

- [ ] 使用合适的文件路径结构
- [ ] 设置文件过期时间
- [ ] 压缩文件（如果需要）

---

## 13. 安全性 ✅

### 13.1 权限控制

- [ ] 验证用户身份（仅教师可导出）
- [ ] 验证课程权限（仅能导出自己的课程）
- [ ] 防止未授权访问

### 13.2 数据安全

- [ ] 敏感数据脱敏（如果需要）
- [ ] 文件访问控制
- [ ] 防止路径遍历攻击

### 13.3 并发控制

- [ ] 同一课程同时只能有一个导出任务
- [ ] 使用数据库锁或分布式锁

---

## 14. 错误处理 ✅

### 14.1 后端错误处理

- [ ] 数据库错误处理
- [ ] OSS错误处理
- [ ] Excel生成错误处理
- [ ] 统一错误响应格式

### 14.2 前端错误处理

- [ ] 网络错误提示
- [ ] 任务失败提示
- [ ] 重试机制

---

## 15. 文档和日志 ✅

### 15.1 代码注释

- [ ] 所有公共方法添加JSDoc注释
- [ ] 复杂逻辑添加行内注释

### 15.2 日志记录

- [ ] 记录关键操作（创建任务、生成文件、上传OSS）
- [ ] 记录错误信息
- [ ] 记录性能指标（执行时间、文件大小）

### 15.3 API文档

- [ ] 使用Swagger/OpenAPI生成API文档
- [ ] 或手动编写API文档

---

## 16. 部署和监控 ✅

### 16.1 部署检查

- [ ] 数据库迁移脚本
- [ ] 环境变量配置
- [ ] OSS配置

### 16.2 监控

- [ ] 任务执行时间监控
- [ ] 任务成功率监控
- [ ] 文件大小监控
- [ ] 错误率监控

---

## 17. 后续优化 📝

### 17.1 功能增强

- [ ] 支持更多导出格式（PDF、CSV）
- [ ] 支持自定义字段选择
- [ ] 支持批量导出
- [ ] 支持邮件发送

### 17.2 性能优化

- [ ] 使用消息队列处理异步任务
- [ ] 使用Redis缓存任务状态
- [ ] 使用CDN加速文件下载

---

## 📊 进度跟踪

| 模块         | 状态 | 完成度 |
| ------------ | ---- | ------ |
| 数据库层     | ⏳   | 0%     |
| 类型定义层   | ⏳   | 0%     |
| Repository层 | ⏳   | 0%     |
| Service层    | ⏳   | 0%     |
| Executor层   | ⏳   | 0%     |
| Controller层 | ⏳   | 0%     |
| Excel生成    | ⏳   | 0%     |
| OSS集成      | ⏳   | 0%     |
| 前端实现     | ⏳   | 0%     |
| 测试         | ⏳   | 0%     |
| 文档         | ✅   | 100%   |

**图例**: ⏳ 未开始 | 🚧 进行中 | ✅ 已完成

---

## 🎯 下一步行动

1. **创建数据库表** - 执行SQL脚本
2. **创建类型定义** - 定义所有TypeScript接口
3. **实现Repository层** - 数据访问层
4. **实现Service层** - 业务逻辑层
5. **实现Executor层** - 异步任务执行
6. **实现Controller层** - HTTP接口
7. **前端集成** - UI组件和API调用
8. **测试和优化** - 确保功能正常

---

## 📞 联系和支持

如有问题，请参考:

- 技术方案文档: `docs/课程考勤数据分享功能技术方案.md`
- 实现指南: `docs/课程考勤数据分享功能实现指南.md`
- 代码规范: `docs/代码规范.md`
