# 课程日历功能 - 分享人列表搜索和同步总结

## 📋 任务概述

为课程日历的分享人列表页面添加搜索和同步功能，提升用户体验和数据管理效率。

---

## ✅ 完成的功能

### 任务1：添加搜索功能

**功能描述**：
- 在分享人列表表格上方添加搜索输入框
- 支持按学生姓名和学号（用户ID）进行实时搜索
- 客户端过滤，无需后端请求
- 模糊匹配，大小写不敏感

**实现位置**：
- 文件：`apps/agendaedu-web/src/routes/_authenticated/course-calendar/index.tsx`
- 位置：分享人列表 Tab（`<TabsContent value='participants'>`）

### 任务2：添加同步功能

**功能描述**：
- 从教学班表（`icalink_teaching_class`）同步学生到日历权限
- 批量添加缺失的学生权限（默认角色：reader）
- 显示同步结果统计（总学生数、已有权限数、新增数量）
- 同步成功后自动刷新分享人列表

**实现位置**：
- 后端服务：`apps/app-icalink/src/services/CourseCalendarService.ts`
- 后端控制器：`apps/app-icalink/src/controllers/CourseCalendarController.ts`
- 前端页面：`apps/agendaedu-web/src/routes/_authenticated/course-calendar/index.tsx`

---

## 🔧 技术实现

### 1. 后端实现

#### 1.1 服务接口定义

**文件**：`apps/app-icalink/src/services/interfaces/ICourseCalendarService.ts`

**新增接口**：
```typescript
export interface CalendarSyncResult {
  totalStudents: number        // 教学班总学生数
  existingPermissions: number  // 已有权限数
  addedCount: number          // 新增权限数
  failedCount: number         // 失败数量
}
```

**新增方法声明**：
```typescript
syncCalendarParticipants(calendarId: string): Promise<ServiceResult<CalendarSyncResult>>
```

#### 1.2 服务实现

**文件**：`apps/app-icalink/src/services/CourseCalendarService.ts`

**核心逻辑**：
1. 验证日历ID并获取课程代码
2. 从 `icalink_teaching_class` 表查询所有学生（按课程代码）
3. 从 WPS 日历 API 获取现有权限列表
4. 使用 Set 数据结构快速查找缺失的学生
5. 批量创建缺失学生的日历权限（角色：reader）
6. 返回同步统计结果

**关键代码片段**：
```typescript
async syncCalendarParticipants(
  calendarId: string
): Promise<ServiceResult<CalendarSyncResult>> {
  try {
    // 1. 验证日历并获取课程代码
    const calendar = await this.calendarRepository.findById(calendarId)
    if (!calendar) {
      return ServiceResult.failure('日历不存在', 404)
    }

    // 2. 查询教学班所有学生
    const teachingClassRecords = await this.vTeachingClassRepository.findByCourseCode(
      calendar.course_code
    )

    // 3. 获取现有权限
    const existingPermissions = await this.calendarAdapter.getCalendarPermissions({
      calendar_id: calendarId,
    })

    // 4. 找出缺失的学生
    const existingUserIds = new Set(existingPermissions.items.map(p => p.user_id))
    const missingStudents = teachingClassRecords.filter(
      record => !existingUserIds.has(record.student_id)
    )

    // 5. 批量创建权限
    if (missingStudents.length > 0) {
      const permissions = missingStudents.map(student => ({
        user_id: student.student_id,
        role: 'reader' as const,
      }))

      await this.calendarAdapter.batchCreateCalendarPermissions({
        calendar_id: calendarId,
        permissions,
      })
    }

    // 6. 返回统计结果
    return ServiceResult.success({
      totalStudents: teachingClassRecords.length,
      existingPermissions: existingPermissions.items.length,
      addedCount: missingStudents.length,
      failedCount: 0,
    })
  } catch (error) {
    this.logger.error('同步日历参与者失败', { error, calendarId })
    return ServiceResult.failure('同步日历参与者失败', 500)
  }
}
```

**性能优化**：
- 使用 `Set` 数据结构进行 O(1) 查找
- 批量创建权限，减少 API 调用次数
- WPS Calendar Adapter 自动处理大批量（100个/批）

#### 1.3 控制器路由

**文件**：`apps/app-icalink/src/controllers/CourseCalendarController.ts`

**新增路由**：
```typescript
@Post('/api/icalink/v1/course-calendar/:calendar_id/sync-participants')
async syncCalendarParticipants(
  request: FastifyRequest<{ Params: { calendar_id: string } }>,
  reply: FastifyReply
): Promise<void> {
  const { calendar_id } = request.params

  const result = await this.courseCalendarService.syncCalendarParticipants(calendar_id)

  if (result.success && result.data) {
    reply.code(200).send({
      success: true,
      data: result.data,
      message: '同步成功',
    })
  } else {
    reply.code(result.statusCode || 500).send({
      success: false,
      message: result.message,
    })
  }
}
```

---

### 2. 前端实现

#### 2.1 类型定义

**文件**：`apps/agendaedu-web/src/types/course-calendar.types.ts`

**新增类型**：
```typescript
export interface CalendarSyncResult {
  totalStudents: number        // 教学班总学生数
  existingPermissions: number  // 已有权限数
  addedCount: number          // 新增权限数
  failedCount: number         // 失败数量
}
```

#### 2.2 API 客户端

**文件**：`apps/agendaedu-web/src/api/course-calendar.api.ts`

**新增函数**：
```typescript
export async function syncCalendarParticipants(
  calendarId: string
): Promise<CalendarSyncResult> {
  const response = await apiClient.post<ApiResponse<CalendarSyncResult>>(
    `/api/icalink/v1/course-calendar/${calendarId}/sync-participants`
  )

  if (!response.success || !response.data) {
    throw new Error(response.message || '同步日历参与者失败')
  }

  return response.data
}
```

#### 2.3 页面组件

**文件**：`apps/agendaedu-web/src/routes/_authenticated/course-calendar/index.tsx`

**新增导入**：
```typescript
import { useMemo, useState } from 'react'
import { toast } from 'sonner'
import { syncCalendarParticipants } from '@/api/course-calendar.api'
```

**新增状态**：
```typescript
const [participantSearchInput, setParticipantSearchInput] = useState('') // 搜索输入
const [isSyncing, setIsSyncing] = useState(false) // 同步状态
```

**查询增强**（添加 refetch）：
```typescript
const {
  data: shareParticipants,
  isLoading: isShareParticipantsLoading,
  refetch: refetchShareParticipants,
} = useQuery({
  queryKey: ['share-participants', selectedCourse?.calendar_id],
  queryFn: () => getCourseShareParticipants(selectedCourse!.calendar_id),
  enabled: !!selectedCourse,
})
```

**搜索过滤逻辑**：
```typescript
const filteredParticipants = useMemo(() => {
  if (!shareParticipants) return []
  if (!participantSearchInput.trim()) return shareParticipants

  const keyword = participantSearchInput.toLowerCase().trim()
  return shareParticipants.filter(
    (p) =>
      p.userId.toLowerCase().includes(keyword) ||
      p.studentName?.toLowerCase().includes(keyword)
  )
}, [shareParticipants, participantSearchInput])
```

**同步处理函数**：
```typescript
const handleSync = async () => {
  if (!selectedCourse?.calendar_id) return

  try {
    setIsSyncing(true)
    const result = await syncCalendarParticipants(selectedCourse.calendar_id)

    // 显示成功提示
    toast.success(`同步成功！新增 ${result.addedCount} 个学生到日历权限`, {
      description: `教学班总学生数: ${result.totalStudents}, 已有权限数: ${result.existingPermissions}`,
    })

    // 刷新分享人列表
    refetchShareParticipants()
  } catch (error) {
    toast.error('同步失败，请重试', {
      description: error instanceof Error ? error.message : '未知错误',
    })
  } finally {
    setIsSyncing(false)
  }
}
```

**UI 组件**：
```tsx
{/* 搜索和同步工具栏 */}
<div className='mb-4 flex items-center gap-2'>
  {/* 搜索框 */}
  <div className='relative flex-1'>
    <Search className='text-muted-foreground absolute top-1/2 left-3 h-4 w-4 -translate-y-1/2' />
    <Input
      placeholder='搜索学生姓名或学号...'
      value={participantSearchInput}
      onChange={(e) => setParticipantSearchInput(e.target.value)}
      className='pl-9'
    />
  </div>

  {/* 同步按钮 */}
  <Button
    onClick={handleSync}
    disabled={isSyncing || !selectedCourse}
    variant='outline'
    className='shrink-0'
  >
    {isSyncing ? (
      <>
        <svg className='mr-2 h-4 w-4 animate-spin' ...>...</svg>
        同步中...
      </>
    ) : (
      <>
        <svg className='mr-2 h-4 w-4' ...>...</svg>
        同步
      </>
    )}
  </Button>
</div>
```

**表格数据绑定**：
```tsx
{filteredParticipants && filteredParticipants.length > 0 ? (
  <Table>
    {/* ... */}
    <TableBody>
      {filteredParticipants.map((participant) => (
        <TableRow key={participant.id}>
          {/* ... */}
        </TableRow>
      ))}
    </TableBody>
  </Table>
) : (
  <p className='text-muted-foreground py-8 text-center text-sm'>
    {participantSearchInput.trim() ? '未找到匹配的学生' : '暂无分享人数据'}
  </p>
)}
```

---

## 📊 功能对比

| 功能项 | 修改前 | 修改后 | 改进 |
|--------|--------|--------|------|
| 搜索功能 | ❌ 无 | ✅ 支持按姓名/学号搜索 | ⬆️ 新增 |
| 搜索方式 | - | ✅ 客户端实时过滤 | ⬆️ 响应快 |
| 同步功能 | ❌ 无 | ✅ 一键同步教学班学生 | ⬆️ 新增 |
| 批量处理 | - | ✅ 自动批量创建权限 | ⬆️ 高效 |
| 同步反馈 | - | ✅ 显示详细统计信息 | ⬆️ 透明 |
| 数据刷新 | ❌ 手动刷新页面 | ✅ 同步后自动刷新 | ⬆️ 便捷 |
| 空状态提示 | ⚠️ 统一提示 | ✅ 区分"无数据"和"搜索无结果" | ⬆️ 友好 |

---

## 🎯 用户体验改进

### 搜索功能

**改进前**：
- ❌ 无法快速查找特定学生
- ❌ 需要手动滚动查看所有数据

**改进后**：
- ✅ 实时搜索，即时反馈
- ✅ 支持姓名和学号双字段搜索
- ✅ 模糊匹配，容错性强
- ✅ 搜索无结果时显示友好提示

### 同步功能

**改进前**：
- ❌ 需要手动为每个学生添加权限
- ❌ 容易遗漏学生
- ❌ 操作繁琐，效率低

**改进后**：
- ✅ 一键同步所有教学班学生
- ✅ 自动识别缺失的学生
- ✅ 批量创建权限，速度快
- ✅ 显示详细统计，结果透明
- ✅ 同步后自动刷新列表

---

## 📋 测试建议

### 搜索功能测试

- [ ] 输入学生姓名，验证过滤结果
- [ ] 输入学号，验证过滤结果
- [ ] 输入部分关键词，验证模糊匹配
- [ ] 输入不存在的关键词，验证"未找到匹配的学生"提示
- [ ] 清空搜索框，验证显示所有数据
- [ ] 大小写混合输入，验证不区分大小写

### 同步功能测试

- [ ] 点击同步按钮，验证同步流程
- [ ] 验证同步中状态（按钮禁用、加载动画）
- [ ] 验证同步成功提示（显示新增数量和统计信息）
- [ ] 验证同步后列表自动刷新
- [ ] 验证同步失败时的错误提示
- [ ] 验证没有缺失学生时的同步结果（新增0个）
- [ ] 验证大量学生（100+）的批量同步

### 边界情况测试

- [ ] 未选择课程时，同步按钮应禁用
- [ ] 分享人列表为空时，显示"暂无分享人数据"
- [ ] 搜索无结果时，显示"未找到匹配的学生"
- [ ] 网络错误时，显示错误提示
- [ ] 同步过程中切换课程，验证状态重置

---

## 📁 修改的文件

### 后端文件（3个）

1. **`apps/app-icalink/src/services/interfaces/ICourseCalendarService.ts`**
   - 新增 `CalendarSyncResult` 接口
   - 新增 `syncCalendarParticipants` 方法声明

2. **`apps/app-icalink/src/services/CourseCalendarService.ts`**
   - 添加 `VTeachingClassRepository` 依赖注入
   - 实现 `syncCalendarParticipants` 方法

3. **`apps/app-icalink/src/controllers/CourseCalendarController.ts`**
   - 新增 `POST /api/icalink/v1/course-calendar/:calendar_id/sync-participants` 路由

### 前端文件（3个）

1. **`apps/agendaedu-web/src/types/course-calendar.types.ts`**
   - 新增 `CalendarSyncResult` 类型定义

2. **`apps/agendaedu-web/src/api/course-calendar.api.ts`**
   - 新增 `syncCalendarParticipants` API 函数

3. **`apps/agendaedu-web/src/routes/_authenticated/course-calendar/index.tsx`**
   - 添加搜索和同步相关导入
   - 添加状态变量（`participantSearchInput`、`isSyncing`）
   - 添加 `filteredParticipants` 过滤逻辑
   - 添加 `handleSync` 同步处理函数
   - 添加搜索框和同步按钮 UI
   - 更新表格数据绑定和空状态提示

---

## 🎉 总结

本次更新为课程日历的分享人列表页面添加了搜索和同步两大核心功能：

1. **搜索功能**：支持按学生姓名和学号实时搜索，提升数据查找效率
2. **同步功能**：一键同步教学班学生到日历权限，自动化权限管理流程

这两个功能显著提升了用户体验和操作效率，使课程日历的权限管理更加便捷和智能。

