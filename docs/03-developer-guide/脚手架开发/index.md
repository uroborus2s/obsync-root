# 幻廊之镜前端脚手架概览

本目录面向使用 `@stratix/cli` 前端模板的开发者，目标是让读者独立生成、理解、修改并验收一个后台管理前端项目。

## 适用对象

- 第一次使用 `web-admin` 模板的前端开发者
- 需要基于样板快速扩展业务页的应用团队

## 维护约束

- 本目录聚焦前端脚手架的生成、结构、开发和验收，不展开框架内部实现细节。
- 文档默认以仓库内 CLI 和 `examples/web-admin-preview` 样例作为参考基线。
- 当模板目录、命令或页面组织方式变化时，要同步更新本目录。
